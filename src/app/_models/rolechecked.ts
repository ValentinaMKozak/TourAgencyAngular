
export class RoleChecked {
    public id: string;
    public name: string;
    public checked: boolean;

    constructor(id: string, name: string, checked: boolean) {
            this.id = id;
            this.name = name;
            this.checked = checked;
    }
}
